@using Microsoft.AspNetCore.Http

@{
    ViewBag.Title = "MicroLevelProjectSiteView";
}

<!-- Hidden Inputs required for JS -->
<input type="hidden" id="HDept_id" value="@Context.Session.GetString("DepartmentID")" />
<input type="hidden" id="HLogin_id" value="@Context.Session.GetString("UserId")" />
<input type="hidden" id="HProjectID" value="@Context.Request.Query["ProjectId"]" />
<input type="hidden" id="HProjecttype" value="@Context.Request.Query["type"]" />
<input type="hidden" id="HClientID" value="@Context.Request.Query["clientid"]" />
<input type="hidden" id="HMonth" value="@Context.Request.Query["month"]" />
<input type="hidden" id="HStatus" value="@Context.Request.Query["Status"]" />
<input type="hidden" id="HModelFlag" value="@Context.Request.Query["ModelFlag"]" />
<input type="hidden" id="HFromRange" value="@Context.Request.Query["FromRange"]" />
<input type="hidden" id="HToRange" value="@Context.Request.Query["ToRange"]" />
<input type="hidden" id="HStockType" value="@Context.Request.Query["StockType"]" />
<input type="hidden" id="Huser_id" value="@Context.Session.GetString("UserId")" />

<style>
    /* ══════════════════════════════════════════════════════════════════
       MicrolevelProjectSiteView — Page Styles
       ALL font-size and font-family values use CSS variables from
       _Layout.cshtml so the Settings → Font controls affect this page
       exactly like every other page in the application.
    ══════════════════════════════════════════════════════════════════ */

    .glass-card {
        background: #ffffff;
        border: 1px solid #e2e8f0;
        box-shadow: 0 2px 12px rgba(0,0,0,0.04);
    }

    [data-theme="dark"] .glass-card {
        background: #1e293b;
        border: 1px solid #334155;
        box-shadow: 0 2px 12px rgba(0,0,0,0.2);
    }

    /* KPI input fields — font follows global layout variables */
    .kpi-input {
        width: 100%;
        background: transparent !important;
        border: 1px solid #cbd5e1 !important;
        border-radius: 4px;
        box-shadow: none !important;
        outline: none !important;
        font-weight: 700;
        font-size: var(--font-size-grid);    /* ← dynamic: respects Settings */
        font-family: var(--font-family);     /* ← dynamic: respects Settings */
        color: inherit;
        text-align: right;
        padding: 2px 4px;
        margin: 0;
    }

    [data-theme="dark"] .kpi-input {
        border-color: #475569 !important;
    }

    .kpi-table {
        width: 100%;
        border-collapse: separate;
        border-spacing: 0;
    }

    /* KPI table headers — use grid-header variable (slightly smaller than base) */
    .kpi-table th {
        font-size: var(--font-size-grid-header); /* ← dynamic: respects Settings */
        font-family: var(--font-family);         /* ← dynamic: respects Settings */
        font-weight: 800;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        padding: 6px 4px;
        border-bottom: 2px solid #e2e8f0;
        text-align: center;
    }

    [data-theme="dark"] .kpi-table th {
        border-bottom-color: #334155;
    }

    .kpi-table td {
        padding: 4px 4px;
        border-bottom: 1px dashed #e2e8f0;
        transition: all 0.2s ease;
    }

    [data-theme="dark"] .kpi-table td {
        border-bottom-color: #334155;
    }

    .kpi-table tr:hover td {
        background: rgba(241, 245, 249, 0.4);
    }
    [data-theme="dark"] .kpi-table tr:hover td {
        background: rgba(15, 23, 42, 0.4);
    }

    .kpi-table tr:last-child td {
        border-bottom: none;
    }

    .status-indicator {
        display: inline-flex;
        align-items: center;
        gap: 6px;
    }

    .status-dot {
        width: 8px;
        height: 8px;
        border-radius: 50%;
        box-shadow: 0 0 0 2px rgba(255,255,255,0.5);
    }

    /* Row labels — use grid variable (compact but readable) */
    .row-label {
        font-size: var(--font-size-grid);    /* ← dynamic: respects Settings */
        font-family: var(--font-family);     /* ← dynamic: respects Settings */
        font-weight: 700;
        color: #64748b;
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }
    [data-theme="dark"] .row-label {
        color: #94a3b8;
    }

    .color-avail { color: #0284c7; }
    .color-book  { color: #d97706; }
    .color-block { color: #dc2626; }
    .color-reg   { color: #7c3aed; }
    .color-total { color: #059669; font-weight: 800; }

    [data-theme="dark"] .color-avail { color: #38bdf8; }
    [data-theme="dark"] .color-book  { color: #fbbf24; }
    [data-theme="dark"] .color-block { color: #f87171; }
    [data-theme="dark"] .color-reg   { color: #a78bfa; }
    [data-theme="dark"] .color-total { color: #34d399; }

    .bg-avail { background-color: #0ea5e9; }
    .bg-book  { background-color: #f59e0b; }
    .bg-block { background-color: #ef4444; }
    .bg-reg   { background-color: #8b5cf6; }
    .bg-total { background-color: #10b981; }

    .tab-pane {
        display: none;
        animation: slideUpFade 0.4s cubic-bezier(0.16, 1, 0.3, 1) forwards;
    }
    .tab-pane.active {
        display: block;
    }

    @@keyframes slideUpFade {
        from { opacity: 0; transform: translateY(10px); }
        to   { opacity: 1; transform: translateY(0); }
    }

    .loader-container {
        flex-direction: column;
        align-items: center;
        justify-content: center;
        padding: 40px;
    }
    .loader-spinner {
        width: 40px;
        height: 40px;
        border: 3px solid rgba(79, 70, 229, 0.2);
        border-top-color: #4f46e5;
        border-radius: 50%;
        animation: spin 1s linear infinite;
    }
    @@keyframes spin {
        to { transform: rotate(360deg); }
    }

    /* Modal Styles */
    .modal-overlay {
        backdrop-filter: blur(5px);
        background: rgba(15, 23, 42, 0.6);
    }
    .modal-content {
        box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
    }

    /* Select2 — font follows global layout variables */
    .select2-container--default .select2-selection--single {
        border-radius: 0.5rem;
        border: 1px solid #e2e8f0;
        height: 32px;
        display: flex;
        align-items: center;
        font-size: var(--font-size-grid);    /* ← dynamic: respects Settings */
        font-family: var(--font-family);     /* ← dynamic: respects Settings */
    }
    [data-theme="dark"] .select2-container--default .select2-selection--single {
        background-color: #1e293b;
        border-color: #334155;
    }
    [data-theme="dark"] .select2-container--default .select2-selection--single .select2-selection__rendered {
        color: #e2e8f0;
    }

    /* ══ DataGrid — visual wrapper only (fonts handled globally by _Layout.cshtml) ══
       _Layout.cshtml global rules set font-size, font-family on .dx-datagrid,
       .dx-header-row, .dx-data-row, .dx-filter-row using --font-size-grid,
       --font-size-grid-header, and --font-family CSS variables — same as every
       other page. DO NOT re-declare font-size/font-family here. */
    .dx-datagrid {
        border-radius: 12px;
        overflow: hidden;
        border: 1px solid #e2e8f0;
    }
    [data-theme="dark"] .dx-datagrid {
        border-color: #334155;
    }
    .grid-wrapper {
        min-height: 200px;
    }
</style>

<div class="px-4 py-3 max-w-[1920px] mx-auto">

    <!-- Header & Tabs -->
    <div class="flex border-b border-gray-200 dark:border-gray-700 mb-3 overflow-x-auto">
        <button class="tab-pill active px-4 py-2 border-b-2 font-medium text-sm border-indigo-600 text-indigo-600 whitespace-nowrap flex items-center gap-2 uppercase tracking-wider transition-colors" id="tab_flat" data-target="divFlat">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path></svg>
            Flat
        </button>
        <button class="tab-pill px-4 py-2 border-b-2 border-transparent font-medium text-sm text-gray-500 whitespace-nowrap flex items-center gap-2 uppercase tracking-wider transition-colors" id="tab_plot" data-target="divPlot">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 20l-5.447-2.724A1 1 0 013 16.382V5.618a1 1 0 011.447-.894L9 7m0 13l6-3m-6 3V7m6 10l4.553 2.276A1 1 0 0021 18.382V7.618a1 1 0 00-.553-.894L15 4m0 13V4m0 0L9 7"></path></svg>
            Plot
        </button>
        <button class="tab-pill px-4 py-2 border-b-2 border-transparent font-medium text-sm text-gray-500 whitespace-nowrap flex items-center gap-2 uppercase tracking-wider transition-colors" id="tab_villa" data-target="divVilla">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path></svg>
            Villa
        </button>
    </div>

    <!-- ==================== FLAT PANE ==================== -->
    <div id="divFlat" class="tab-pane active">
        <div class="grid grid-cols-1 xl:grid-cols-12 gap-3 mb-3 items-stretch">
            
            <!-- Filters Card -->
            <div class="glass-card rounded-xl p-2 flex flex-col justify-center gap-2 xl:col-span-3">
                <div class="w-full">
                    <label class="block text-[10px] font-bold text-slate-500 uppercase mb-1">Project</label>
                    <select class="select2 w-full" id="sel_FLAT"></select>
                </div>
                <div class="w-full">
                    <label class="block text-[10px] font-bold text-slate-500 dark:text-slate-400 uppercase mb-1">Project Site</label>
                    <select class="select2 w-full" id="sel_FLAT_SITE">
                        <option value="" disabled selected hidden>-- Select Site --</option>
                    </select>
                </div>
            </div>

            <!-- KPI Matrix Card -->
            <div class="glass-card rounded-xl p-2 overflow-x-auto flex items-center xl:col-span-9">
                <table class="kpi-table min-w-full">
                    <thead>
                        <tr>
                            <th></th>
                            <th><div class="status-indicator color-avail"><span class="status-dot bg-avail"></span>Available</div></th>
                            <th><div class="status-indicator color-book"><span class="status-dot bg-book"></span>Booked</div></th>
                            <th><div class="status-indicator color-block"><span class="status-dot bg-block"></span>Blocked</div></th>
                            <th><div class="status-indicator color-reg"><span class="status-dot bg-reg"></span>Registered</div></th>
                            <th><div class="status-indicator color-total"><span class="status-dot bg-total"></span>Total</div></th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td class="row-label">Count</td>
                            <td><input type="text" id="txt_flat_avail_count" class="kpi-input color-avail" readonly /></td>
                            <td><input type="text" id="txt_flat_book_count" class="kpi-input color-book" readonly /></td>
                            <td><input type="text" id="txt_flat_block_count" class="kpi-input color-block" readonly /></td>
                            <td><input type="text" id="txt_flat_reg_count" class="kpi-input color-reg" readonly /></td>
                            <td><input type="text" id="txt_flat_tot_count" class="kpi-input color-total" readonly /></td>
                        </tr>
                        <tr>
                            <td class="row-label">Area / SQFT</td>
                            <td><input type="text" id="flat_TotalArea_Available_Count" class="kpi-input color-avail" readonly /></td>
                            <td><input type="text" id="flat_TotalArea_Booked_Count" class="kpi-input color-book" readonly /></td>
                            <td><input type="text" id="flat_TotalArea_Blocked_Count" class="kpi-input color-block" readonly /></td>
                            <td><input type="text" id="flat_TotalArea_Register_Count" class="kpi-input color-reg" readonly /></td>
                            <td><input type="text" id="flat_TotalArea_Total_Count" class="kpi-input color-total" readonly /></td>
                        </tr>
                        <tr>
                            <td class="row-label">Cost</td>
                            <td><input type="text" id="txt_flat_avail_total" class="kpi-input color-avail" readonly /></td>
                            <td><input type="text" id="txt_flat_book_total" class="kpi-input color-book" readonly /></td>
                            <td><input type="text" id="txt_flat_block_total" class="kpi-input color-block" readonly /></td>
                            <td><input type="text" id="txt_flat_reg_total" class="kpi-input color-reg" readonly /></td>
                            <td><input type="text" id="txt_flat_tot_total" class="kpi-input color-total" readonly /></td>
                        </tr>
                        <tr>
                            <td class="row-label">Percentage</td>
                            <td><input type="text" id="txt_flat_avail_per" class="kpi-input color-avail" readonly /></td>
                            <td><input type="text" id="txt_flat_book_per" class="kpi-input color-book" readonly /></td>
                            <td><input type="text" id="txt_flat_block_per" class="kpi-input color-block" readonly /></td>
                            <td><input type="text" id="txt_flat_reg_per" class="kpi-input color-reg" readonly /></td>
                            <td><input type="text" id="txt_flat_tot_per" class="kpi-input color-total" readonly /></td>
                        </tr>
                    </tbody>
                </table>
            </div>

        </div>

        <!-- Buttons Row -->
        <div class="flex justify-center flex-wrap gap-4 mb-2 border-b border-slate-200 dark:border-slate-700 pb-2">
            <button id="btn_flat_view" class="btn-load">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"></path></svg>
                Load
            </button>
            <button id="btn_flat_clear" class="btn-clear">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
                Clear
            </button>
        </div>

        <div id="shade_slot_flat" class="loader-container" style="display: none;">
            <div class="loader-spinner mb-3"></div>
            <p class="text-sm font-semibold text-slate-500 dark:text-slate-400">Loading Grid Data...</p>
        </div>

        <div class="glass-card rounded-2xl p-4 grid-wrapper">
            <div id="gridContainer" class="w-full"></div>
        </div>
    </div>

    <!-- ==================== PLOT PANE ==================== -->
    <div id="divPlot" class="tab-pane">
        <div class="grid grid-cols-1 xl:grid-cols-12 gap-3 mb-3 items-stretch">
            
            <!-- Filters Card -->
            <div class="glass-card rounded-xl p-2 flex flex-col justify-center gap-2 xl:col-span-3">
                <div class="w-full">
                    <label class="block text-[10px] font-bold text-slate-500 dark:text-slate-400 uppercase mb-1">Project</label>
                    <select class="select2 w-full" id="sel_PLOT"><option value="--ALL PROJECTS--">--ALL PROJECTS--</option></select>
                </div>
                <div class="w-full">
                    <label class="block text-[10px] font-bold text-slate-500 dark:text-slate-400 uppercase mb-1">Project Site</label>
                    <select class="select2 w-full" id="sel_PLOT_SITE"><option value="" disabled selected hidden>-- Select Site --</option></select>
                </div>
            </div>

            <!-- KPI Matrix Card -->
            <div class="glass-card rounded-xl p-2 overflow-x-auto flex items-center xl:col-span-7">
                <table class="kpi-table min-w-full">
                    <thead>
                        <tr>
                            <th></th>
                            <th><div class="status-indicator color-avail"><span class="status-dot bg-avail"></span>Available</div></th>
                            <th><div class="status-indicator color-book"><span class="status-dot bg-book"></span>Booked</div></th>
                            <th><div class="status-indicator color-block"><span class="status-dot bg-block"></span>Blocked</div></th>
                            <th><div class="status-indicator color-reg"><span class="status-dot bg-reg"></span>Registered</div></th>
                            <th><div class="status-indicator color-total"><span class="status-dot bg-total"></span>Total</div></th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td class="row-label">Count</td>
                            <td><input type="text" id="PLOT_Available_Count" class="kpi-input color-avail" readonly /></td>
                            <td><input type="text" id="PLOT_BOOKED_Count" class="kpi-input color-book" readonly /></td>
                            <td><input type="text" id="PLOT_BLOCKED_Count" class="kpi-input color-block" readonly /></td>
                            <td><input type="text" id="PLOT_REGISTERED_Count" class="kpi-input color-reg" readonly /></td>
                            <td><input type="text" id="PLOT_TOTAL_Count" class="kpi-input color-total" readonly /></td>
                        </tr>
                        <tr>
                            <td class="row-label">SQFT</td>
                            <td><input type="text" id="PLOT_TotalArea_Available_Count" class="kpi-input color-avail" readonly /></td>
                            <td><input type="text" id="PLOT_TotalArea_Booked_Count" class="kpi-input color-book" readonly /></td>
                            <td><input type="text" id="PLOT_TotalArea_Blocked_Count" class="kpi-input color-block" readonly /></td>
                            <td><input type="text" id="PLOT_TotalArea_Register_Count" class="kpi-input color-reg" readonly /></td>
                            <td><input type="text" id="PLOT_TotalArea_Count" class="kpi-input color-total" readonly /></td>
                        </tr>
                        <tr>
                            <td class="row-label">Grounds</td>
                            <td><input type="text" id="PLOT_Available_Grounds" class="kpi-input color-avail" readonly /></td>
                            <td><input type="text" id="PLOT_BOOKED_Grounds" class="kpi-input color-book" readonly /></td>
                            <td><input type="text" id="PLOT_BLOCKED_Grounds" class="kpi-input color-block" readonly /></td>
                            <td><input type="text" id="PLOT_REGISTERED_Grounds" class="kpi-input color-reg" readonly /></td>
                            <td><input type="text" id="PLOT_TOTAL_Grounds" class="kpi-input color-total" readonly /></td>
                        </tr>
                        <tr>
                            <td class="row-label">Cost</td>
                            <td><input type="text" id="PLOT_Available_Cost" class="kpi-input color-avail" readonly /></td>
                            <td><input type="text" id="PLOT_BOOKED_Cost" class="kpi-input color-book" readonly /></td>
                            <td><input type="text" id="PLOT_BLOCKED_Cost" class="kpi-input color-block" readonly /></td>
                            <td><input type="text" id="PLOT_REGISTERED_Cost" class="kpi-input color-reg" readonly /></td>
                            <td><input type="text" id="PLOT_TOTAL_Cost" class="kpi-input color-total" readonly /></td>
                        </tr>
                        <tr>
                            <td class="row-label">Percentage</td>
                            <td><input type="text" id="PLOT_Available_Percen" class="kpi-input color-avail" readonly /></td>
                            <td><input type="text" id="PLOT_BOOKED_Percen" class="kpi-input color-book" readonly /></td>
                            <td><input type="text" id="PLOT_BLOCKED_Percen" class="kpi-input color-block" readonly /></td>
                            <td><input type="text" id="PLOT_REGISTERED_Percen" class="kpi-input color-reg" readonly /></td>
                            <td><input type="text" id="PLOT_TOTAL_Percen" class="kpi-input color-total" readonly /></td>
                        </tr>
                    </tbody>
                </table>
            </div>
            
            <!-- NPV Card -->
            <div class="glass-card rounded-xl p-2 flex flex-col justify-center gap-2 xl:col-span-2">
                <div class="bg-slate-50 dark:bg-slate-800/50 p-1.5 rounded-lg border border-slate-100 dark:border-slate-700 flex flex-col items-center justify-center gap-1">
                    <span class="text-[10px] font-bold text-slate-500 dark:text-slate-400 uppercase">NPV P/L</span>
                    <input type="text" id="PLOT_NPV_PROFIT_LOSS" class="bg-transparent border-none font-bold text-sm text-center text-indigo-600 dark:text-indigo-400 outline-none w-full p-0 m-0" readonly />
                </div>
                <div class="bg-slate-50 dark:bg-slate-800/50 p-1.5 rounded-lg border border-slate-100 dark:border-slate-700 flex flex-col items-center justify-center gap-1">
                    <span class="text-[10px] font-bold text-slate-500 dark:text-slate-400 uppercase">NPV %</span>
                    <input type="text" id="PLOT_NPV_PERCENTAGE" class="bg-transparent border-none font-bold text-sm text-center text-indigo-600 dark:text-indigo-400 outline-none w-full p-0 m-0" readonly />
                </div>
            </div>

        </div>

        <!-- Buttons Row -->
        <div class="flex justify-center flex-wrap gap-4 mb-2 border-b border-slate-200 dark:border-slate-700 pb-2">
            <button id="btn_plot_view" class="btn-load">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"></path></svg>
                Load
            </button>
            <button id="btn_plot_layout" data-type="PLOT" class="layout_common btn-apply">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 20l-5.447-2.724A1 1 0 013 16.382V5.618a1 1 0 011.447-.894L9 7m0 13l6-3m-6 3V7m6 10l4.553 2.276A1 1 0 0021 18.382V7.618a1 1 0 00-.553-.894L15 4m0 13V4m0 0L9 7"></path></svg>
                Layout
            </button>
            <button id="btn_plot_cmda" data-type="PLOT" class="btn-apply">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path></svg>
                CMDA Layout
            </button>
            <button id="btn_plot_Atta" data-type="PLOT" class="btn-apply">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.172 7l-6.586 6.586a2 2 0 102.828 2.828l6.414-6.586a4 4 0 00-5.656-5.656l-6.415 6.585a6 6 0 108.486 8.486L20.5 13"></path></svg>
                Attachments
            </button>
            <button id="btn_plot_clear" class="btn-clear">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
                Clear
            </button>
        </div>

        <div id="shade_slot_plot" class="loader-container" style="display: none;">
            <div class="loader-spinner mb-3"></div>
            <p class="text-sm font-semibold text-slate-500 dark:text-slate-400">Loading Grid Data...</p>
        </div>

        <div class="glass-card rounded-2xl p-4 grid-wrapper">
            <div id="gridPlottbl" class="w-full"></div>
        </div>
    </div>


    <!-- ==================== VILLA PANE ==================== -->
    <div id="divVilla" class="tab-pane">
        <div class="grid grid-cols-1 xl:grid-cols-12 gap-3 mb-3 items-stretch">
            
            <!-- Filters Card -->
            <div class="glass-card rounded-xl p-2 flex flex-col justify-center gap-2 xl:col-span-3">
                <div class="w-full">
                    <label class="block text-[10px] font-bold text-slate-500 dark:text-slate-400 uppercase mb-1">Project</label>
                    <select class="select2 w-full" id="sel_VILLA"></select>
                </div>
                <div class="w-full">
                    <label class="block text-[10px] font-bold text-slate-500 dark:text-slate-400 uppercase mb-1">Project Site</label>
                    <select class="select2 w-full" id="sel_VILLA_SITE"><option value="" disabled selected hidden>-- Select Site --</option></select>
                </div>
            </div>

            <!-- KPI Matrix Card -->
            <div class="glass-card rounded-xl p-2 overflow-x-auto flex items-center xl:col-span-9">
                <table class="kpi-table min-w-full">
                    <thead>
                        <tr>
                            <th></th>
                            <th><div class="status-indicator color-avail"><span class="status-dot bg-avail"></span>Available</div></th>
                            <th><div class="status-indicator color-book"><span class="status-dot bg-book"></span>Booked</div></th>
                            <th><div class="status-indicator color-block"><span class="status-dot bg-block"></span>Blocked</div></th>
                            <th><div class="status-indicator color-reg"><span class="status-dot bg-reg"></span>Registered</div></th>
                            <th><div class="status-indicator color-total"><span class="status-dot bg-total"></span>Total</div></th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td class="row-label">Count</td>
                            <td><input type="text" id="villa_avail_count" class="kpi-input color-avail" readonly /></td>
                            <td><input type="text" id="villa_book_count" class="kpi-input color-book" readonly /></td>
                            <td><input type="text" id="villa_block_count" class="kpi-input color-block" readonly /></td>
                            <td><input type="text" id="villa_reg_count" class="kpi-input color-reg" readonly /></td>
                            <td><input type="text" id="villa_tot_count" class="kpi-input color-total" readonly /></td>
                        </tr>
                        <tr>
                            <td class="row-label">SQFT</td>
                            <td><input type="text" id="villa_TotalArea_Available_Count" class="kpi-input color-avail" readonly /></td>
                            <td><input type="text" id="villa_TotalArea_Booked_Count" class="kpi-input color-book" readonly /></td>
                            <td><input type="text" id="villa_TotalArea_Blocked_Count" class="kpi-input color-block" readonly /></td>
                            <td><input type="text" id="villa_TotalArea_registered_Count" class="kpi-input color-reg" readonly /></td>
                            <td><input type="text" id="villa_TotalArea_Total_Count" class="kpi-input color-total" readonly /></td>
                        </tr>
                        <tr>
                            <td class="row-label">Cost</td>
                            <td><input type="text" id="villa_avail_amt" class="kpi-input color-avail" readonly /></td>
                            <td><input type="text" id="villa_book_amt" class="kpi-input color-book" readonly /></td>
                            <td><input type="text" id="villa_block_amt" class="kpi-input color-block" readonly /></td>
                            <td><input type="text" id="villa_reg_amt" class="kpi-input color-reg" readonly /></td>
                            <td><input type="text" id="villa_tot_amt" class="kpi-input color-total" readonly /></td>
                        </tr>
                        <tr>
                            <td class="row-label">Percentage</td>
                            <td><input type="text" id="villa_avail_per" class="kpi-input color-avail" readonly /></td>
                            <td><input type="text" id="villa_book_per" class="kpi-input color-book" readonly /></td>
                            <td><input type="text" id="villa_block_per" class="kpi-input color-block" readonly /></td>
                            <td><input type="text" id="villa_reg_per" class="kpi-input color-reg" readonly /></td>
                            <td><input type="text" id="villa_tot_per" class="kpi-input color-total" readonly /></td>
                        </tr>
                    </tbody>
                </table>
            </div>

        </div>

        <!-- Buttons Row -->
        <div class="flex justify-center flex-wrap gap-4 mb-2 border-b border-slate-200 dark:border-slate-700 pb-2">
            <button id="btn_villa_view" class="btn-load">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"></path></svg>
                Load
            </button>
            <button id="btn_villa_layout" data-type="VILLA" class="layout_common btn-apply">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 20l-5.447-2.724A1 1 0 013 16.382V5.618a1 1 0 011.447-.894L9 7m0 13l6-3m-6 3V7m6 10l4.553 2.276A1 1 0 0021 18.382V7.618a1 1 0 00-.553-.894L15 4m0 13V4m0 0L9 7"></path></svg>
                Layout
            </button>
            <button id="btn_Villa_Atta" data-type="VILLA" class="btn-apply">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.172 7l-6.586 6.586a2 2 0 102.828 2.828l6.414-6.586a4 4 0 00-5.656-5.656l-6.415 6.585a6 6 0 108.486 8.486L20.5 13"></path></svg>
                Attachments
            </button>
            <button id="btn_villa_clear" class="btn-clear">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
                Clear
            </button>
        </div>

        <div id="shade_slot_villa" class="loader-container" style="display: none;">
            <div class="loader-spinner mb-3"></div>
            <p class="text-sm font-semibold text-slate-500 dark:text-slate-400">Loading Grid Data...</p>
        </div>

        <div class="glass-card rounded-2xl p-4 grid-wrapper">
            <div id="grid_Villa" class="w-full"></div>
        </div>
    </div>
</div>

<!-- IMG Popup Modal -->
<div id="imgpopup" class="fixed inset-0 z-50 hidden modal-overlay items-center justify-center p-4">
    <div class="modal-content bg-white dark:bg-slate-900 rounded-2xl w-full max-w-6xl max-h-[90vh] overflow-hidden flex flex-col">
        <div class="px-6 py-4 border-b border-slate-200 dark:border-slate-800 flex justify-between items-center">
            <h3 class="text-lg font-bold text-slate-800 dark:text-white">Image Preview</h3>
            <button class="text-slate-400 hover:text-slate-600 transition-colors close-modal" data-modal="imgpopup">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
            </button>
        </div>
        <div class="p-6 overflow-y-auto flex-1">
            <div class="flex flex-col lg:flex-row gap-6">
                <!-- Image container -->
                <div class="w-full lg:w-2/3">
                    <div class="border border-slate-200 dark:border-slate-700 rounded-xl overflow-auto" style="height: 500px; position:relative;">
                        <div id="picture">
                            <img src="~/images/ch40.jpeg" alt="Layout Image" id="image" class="max-w-none" style="width: 1131px; height: 1600px;">
                        </div>
                    </div>
                </div>
                
                <!-- Controls -->
                <div class="w-full lg:w-1/3 flex flex-col gap-6">
                    <div class="grid grid-cols-2 gap-4">
                        <div class="bg-slate-50 dark:bg-slate-800 p-3 rounded-xl border border-slate-100 dark:border-slate-700">
                            <div class="text-xs font-bold text-slate-500 dark:text-slate-400 uppercase">Available</div>
                            <div class="text-xl font-black text-emerald-500" id="layoutavailable">0</div>
                        </div>
                        <div class="bg-slate-50 dark:bg-slate-800 p-3 rounded-xl border border-slate-100 dark:border-slate-700">
                            <div class="text-xs font-bold text-slate-500 dark:text-slate-400 uppercase">Booked</div>
                            <div class="text-xl font-black text-amber-500" id="layoutbooked">0</div>
                        </div>
                        <div class="bg-slate-50 dark:bg-slate-800 p-3 rounded-xl border border-slate-100 dark:border-slate-700">
                            <div class="text-xs font-bold text-slate-500 dark:text-slate-400 uppercase">Registered</div>
                            <div class="text-xl font-black text-purple-500" id="layoutregistrated">0</div>
                        </div>
                        <div class="bg-slate-50 dark:bg-slate-800 p-3 rounded-xl border border-slate-100 dark:border-slate-700">
                            <div class="text-xs font-bold text-slate-500 dark:text-slate-400 uppercase">Blocked</div>
                            <div class="text-xl font-black text-rose-500" id="layoutblocked">0</div>
                        </div>
                        <div class="bg-slate-100 dark:bg-slate-700 p-3 rounded-xl border border-slate-200 dark:border-slate-600 col-span-2">
                            <div class="text-xs font-bold text-slate-600 dark:text-slate-300 uppercase">Total</div>
                            <div class="text-2xl font-black text-slate-900 dark:text-white" id="layouttotal">0</div>
                        </div>
                    </div>

                    <div class="space-y-3">
                        <button onclick="exportPicture()" class="btn-load w-full">CONVERT TO IMG</button>
                        <canvas id="canvas" class="hidden"></canvas>
                        <button class="btn-clear w-full open-modal" data-target="layoutpopup">LAYOUT</button>
                    </div>
                    
                    <div class="glass-card p-4 rounded-xl space-y-3">
                        <label class="flex items-center gap-3 cursor-pointer">
                            <input type="checkbox" name="chklayoucheckbox" id="chklayoutavailable" class="w-4 h-4 rounded text-emerald-500 focus:ring-emerald-500 border-gray-300">
                            <span class="text-sm font-semibold text-slate-700 dark:text-slate-300">Available</span>
                        </label>
                        <label class="flex items-center gap-3 cursor-pointer">
                            <input type="checkbox" name="chklayoucheckbox" value="2" id="chklayoutbooked" class="w-4 h-4 rounded text-amber-500 focus:ring-amber-500 border-gray-300">
                            <span class="text-sm font-semibold text-slate-700 dark:text-slate-300">Booked</span>
                        </label>
                        <label class="flex items-center gap-3 cursor-pointer">
                            <input type="checkbox" name="chklayoucheckbox" value="3" id="chklayoutregistered" class="w-4 h-4 rounded text-purple-500 focus:ring-purple-500 border-gray-300">
                            <span class="text-sm font-semibold text-slate-700 dark:text-slate-300">Registered</span>
                        </label>
                        <label class="flex items-center gap-3 cursor-pointer">
                            <input type="checkbox" name="chklayoucheckbox" value="4" id="chklayoutblocked" class="w-4 h-4 rounded text-rose-500 focus:ring-rose-500 border-gray-300">
                            <span class="text-sm font-semibold text-slate-700 dark:text-slate-300">Blocked</span>
                        </label>
                    </div>
                    
                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="block text-xs font-bold text-slate-500 dark:text-slate-400 uppercase mb-1">X Axis</label>
                            <input type="text" id="layoutxaxis" class="w-full rounded-lg border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-800 p-2 text-sm focus:ring-2 focus:ring-indigo-500" />
                        </div>
                        <div>
                            <label class="block text-xs font-bold text-slate-500 dark:text-slate-400 uppercase mb-1">Y Axis</label>
                            <input type="text" id="layoutyaxis" class="w-full rounded-lg border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-800 p-2 text-sm focus:ring-2 focus:ring-indigo-500" />
                        </div>
                    </div>
                    
                    <div>
                        <label class="block text-xs font-bold text-slate-500 dark:text-slate-400 uppercase mb-1">Search Plot No</label>
                        <div class="flex gap-2">
                            <select id="layoutplotno" class="flex-1 rounded-lg border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-800 p-2 text-sm focus:ring-2 focus:ring-indigo-500">
                                <option>--Select--</option>
                            </select>
                            <button class="btn-load">FIND</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Layout Popup Modal -->
<div id="layoutpopup" class="fixed inset-0 z-50 hidden modal-overlay items-center justify-center p-4">
    <div class="modal-content bg-white dark:bg-slate-900 rounded-2xl w-full max-w-4xl max-h-[90vh] overflow-hidden flex flex-col">
        <div class="px-6 py-4 border-b border-slate-200 dark:border-slate-800 flex justify-between items-center bg-slate-50 dark:bg-slate-800/50">
            <h3 class="text-lg font-bold text-slate-800 dark:text-white">Layout Management</h3>
            <button class="text-slate-400 hover:text-slate-600 transition-colors close-modal" data-modal="layoutpopup">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
            </button>
        </div>
        <div class="p-6 overflow-y-auto flex-1">
            <div class="mb-6 bg-indigo-50 dark:bg-indigo-900/20 p-4 rounded-xl border border-indigo-100 dark:border-indigo-800/30">
                <span class="text-xs font-bold text-indigo-400 uppercase tracking-widest">Project</span>
                <div id="layoutprojectname" class="text-lg font-black text-indigo-700 dark:text-indigo-300"></div>
            </div>

            <div class="flex flex-col md:flex-row gap-6 items-end mb-8">
                <div class="flex-1">
                    <label class="block text-sm font-bold text-slate-700 dark:text-slate-300 mb-2">Layout Image</label>
                    <div id="layoutiamges" class="flex items-center gap-4">
                        <a href="" target="_blank" class="text-indigo-600 hover:text-indigo-700 font-semibold underline underline-offset-2">View Current</a>
                        <button id="btn_attachlayout" class="btn-clear">
                            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.172 7l-6.586 6.586a2 2 0 102.828 2.828l6.414-6.586a4 4 0 00-5.656-5.656l-6.415 6.585a6 6 0 108.486 8.486L20.5 13"></path></svg>
                            Upload New
                        </button>
                        <input type="file" accept="" id="entry_filelayout" name="myFile" class="hidden">
                    </div>
                </div>
                <button id="btnsavelayoutaxis" class="btn-load">SAVE AXIS</button>
            </div>

            <div class="border border-slate-200 dark:border-slate-700 rounded-xl overflow-hidden">
                <table id="layoutentry_table" class="min-w-full divide-y divide-slate-200 dark:divide-slate-700">
                    <thead class="bg-slate-50 dark:bg-slate-800/50">
                        <tr>
                            <th class="px-4 py-3 text-left text-xs font-bold text-slate-500 dark:text-slate-400 uppercase tracking-wider">Flat No</th>
                            <th class="px-4 py-3 text-left text-xs font-bold text-slate-500 dark:text-slate-400 uppercase tracking-wider">Status</th>
                            <th class="px-4 py-3 text-left text-xs font-bold text-slate-500 dark:text-slate-400 uppercase tracking-wider">X.Axis</th>
                            <th class="px-4 py-3 text-left text-xs font-bold text-slate-500 dark:text-slate-400 uppercase tracking-wider">Y.Axis</th>
                            <th hidden></th>
                            <th hidden></th>
                            <th hidden></th>
                        </tr>
                    </thead>
                    <tbody class="bg-white dark:bg-slate-900 divide-y divide-slate-200 dark:divide-slate-700"></tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<!-- Attachments Modals -->
<div id="attachModal" class="fixed inset-0 z-50 hidden modal-overlay items-center justify-center p-4">
    <input hidden id="HModProjectId">
    <input hidden id="HModPlottranid">
    <div class="modal-content bg-white dark:bg-slate-900 rounded-2xl w-full max-w-4xl max-h-[90vh] overflow-hidden flex flex-col">
        <div class="px-6 py-4 border-b border-slate-200 dark:border-slate-800 flex justify-between items-center">
            <h3 class="text-lg font-bold text-slate-800 dark:text-white">Customer Documents</h3>
            <button class="text-slate-400 hover:text-slate-600 transition-colors close-modal" data-modal="attachModal">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
            </button>
        </div>
        <div class="p-6 overflow-y-auto flex-1">
            <div class="border border-slate-200 dark:border-slate-700 rounded-xl overflow-hidden">
                <table id="tbl_CustDOCUMENTS" class="min-w-full divide-y divide-slate-200 dark:divide-slate-700">
                    <thead class="bg-slate-50 dark:bg-slate-800/50">
                        <tr>
                            <th class="px-4 py-3 text-left text-xs font-bold text-slate-500 dark:text-slate-400 uppercase tracking-wider">Type</th>
                            <th class="px-4 py-3 text-left text-xs font-bold text-slate-500 dark:text-slate-400 uppercase tracking-wider">Name</th>
                            <th class="px-4 py-3 text-left text-xs font-bold text-slate-500 dark:text-slate-400 uppercase tracking-wider">File</th>
                            <th class="px-4 py-3 text-left text-xs font-bold text-slate-500 dark:text-slate-400 uppercase tracking-wider">View</th>
                            <th hidden>DOCTRANID</th>
                        </tr>
                    </thead>
                    <tbody class="bg-white dark:bg-slate-900 divide-y divide-slate-200 dark:divide-slate-700"></tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<div id="attahModal" class="fixed inset-0 z-50 hidden modal-overlay items-center justify-center p-4">
    <input hidden id="HAttaProjectId">
    <div class="modal-content bg-white dark:bg-slate-900 rounded-2xl w-full max-w-5xl max-h-[90vh] overflow-hidden flex flex-col">
        <div class="px-6 py-4 border-b border-slate-200 dark:border-slate-800 flex justify-between items-center bg-slate-50 dark:bg-slate-800/50">
            <h3 class="text-lg font-bold text-slate-800 dark:text-white">Project Attachments</h3>
            <button class="text-slate-400 hover:text-slate-600 transition-colors close-modal" data-modal="attahModal">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
            </button>
        </div>
        <div class="p-6 overflow-y-auto flex-1">
            <div class="glass-card p-5 rounded-xl mb-6 flex flex-col md:flex-row gap-4 items-end">
                <div class="flex-1">
                    <label class="block text-xs font-bold text-slate-500 dark:text-slate-400 uppercase mb-2">Attachment Type <span class="text-red-500">*</span></label>
                    <select id="dd_AttaType" class="w-full rounded-lg border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-800 p-2.5 text-sm focus:ring-2 focus:ring-indigo-500">
                        <option value='0'>--Select--</option>
                        <option value='FAQ'>FAQ</option>
                        <option value='COMPARISON'>COMPARISON</option>
                    </select>
                </div>
                <button id="btn_Gadd_row2" class="btn-load">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path></svg>
                    ADD DOCUMENT
                </button>
                <input type="file" id="entry_fileG2" name="myFileG2" class="hidden">
            </div>

            <div class="border border-slate-200 dark:border-slate-700 rounded-xl overflow-hidden">
                <table id="tbl_Attadocuments" class="min-w-full divide-y divide-slate-200 dark:divide-slate-700">
                    <thead class="bg-slate-50 dark:bg-slate-800/50">
                        <tr>
                            <th class="px-4 py-3 text-left text-xs font-bold text-slate-500 dark:text-slate-400 uppercase tracking-wider">Type</th>
                            <th class="px-4 py-3 text-left text-xs font-bold text-slate-500 dark:text-slate-400 uppercase tracking-wider">Name</th>
                            <th class="px-4 py-3 text-left text-xs font-bold text-slate-500 dark:text-slate-400 uppercase tracking-wider">File</th>
                            <th class="px-4 py-3 text-left text-xs font-bold text-slate-500 dark:text-slate-400 uppercase tracking-wider">View</th>
                            <th hidden>DOCTRANID</th>
                            <th class="px-4 py-3 text-left text-xs font-bold text-slate-500 dark:text-slate-400 uppercase tracking-wider">Action</th>
                        </tr>
                    </thead>
                    <tbody class="bg-white dark:bg-slate-900 divide-y divide-slate-200 dark:divide-slate-700"></tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<script src="~/js/ApplicationJs/VGN_Utils.js"></script>
<script src="~/js/applicationjs/MicrolevelProjectSIteView.js"></script>

@section Scripts {
   
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            // Pill tabs switching
            const tabs = document.querySelectorAll('.tab-pill');
            const panes = document.querySelectorAll('.tab-pane');

            tabs.forEach(tab => {
                tab.addEventListener('click', () => {
                    // Reset all tabs
                    tabs.forEach(t => {
                        t.classList.remove('active', 'border-indigo-600', 'text-indigo-600');
                        t.classList.add('border-transparent', 'text-gray-500');
                    });
                    // Set current tab active
                    tab.classList.add('active', 'border-indigo-600', 'text-indigo-600');
                    tab.classList.remove('border-transparent', 'text-gray-500');

                    // Hide all panes
                    panes.forEach(p => p.classList.remove('active'));
                    // Show target pane
                    const target = tab.getAttribute('data-target');
                    const pane = document.getElementById(target);
                    if(pane) {
                        pane.classList.add('active');
                        // Trigger a resize event to ensure DevExtreme grids layout correctly when made visible
                        setTimeout(() => { window.dispatchEvent(new Event('resize')); }, 50);
                    }
                });
            });

            // Initialize Select2 with cleaner look
            $('.select2').select2({
                width: '100%',
                dropdownCssClass: 'rounded-xl border-slate-200 shadow-xl overflow-hidden text-sm',
            });

            // Simple modal toggling (to replace bootstrap data-toggle/data-target where possible)
            // Note: The JS files might still rely on jQuery modal like $('#imgpopup').modal('show') 
            // We ensure classes are toggled or override the modal behavior if needed.
            // But if they use bootstrap JS, these manual toggles help fallback.
            
            function openModal(id) {
                const el = document.getElementById(id);
                if(el) {
                    el.classList.remove('hidden');
                    el.classList.add('flex');
                    document.body.style.overflow = 'hidden';
                }
            }
            
            function closeModal(id) {
                const el = document.getElementById(id);
                if(el) {
                    el.classList.add('hidden');
                    el.classList.remove('flex');
                    document.body.style.overflow = '';
                }
            }

            document.querySelectorAll('.open-modal').forEach(btn => {
                btn.addEventListener('click', (e) => {
                    const target = e.currentTarget.getAttribute('data-target');
                    openModal(target);
                });
            });

            document.querySelectorAll('.close-modal').forEach(btn => {
                btn.addEventListener('click', (e) => {
                    const target = e.currentTarget.getAttribute('data-modal');
                    closeModal(target);
                });
            });

            // Patch for jQuery modal if used in external JS to use our classes
            if (window.jQuery) {
                const origModal = $.fn.modal;
                $.fn.modal = function(action) {
                    if (action === 'show') {
                        this.removeClass('hidden').addClass('flex');
                        document.body.style.overflow = 'hidden';
                    } else if (action === 'hide') {
                        this.addClass('hidden').removeClass('flex');
                        document.body.style.overflow = '';
                    } else {
                        // Fallback
                        return origModal.apply(this, arguments);
                    }
                };
            }
        });
    </script>
}